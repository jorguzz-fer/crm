# Schema changes log (append-only)

Registre aqui toda mudança proposta/aplicada ao `packages/db/prisma/schema.prisma`. Datar + sessão autora + descrição curta + status.

Formato: `YYYY-MM-DD | <session> | <status> | <resumo>`

Status: `proposed` → `reviewed` → `applied` → `rolled-back` (se necessário).

---

2026-04-23 | sdr-scaffold | proposed | FASE 0 scaffold: sem mudanças de schema nesta fase. Modelos novos (Attribution, ConversionEvent, Transaction, Campaign, Product, FollowupSequence) propostos nas fases 3–4 — documentados em IMPLEMENTATION_PLAN.md.

[2026-04-23] Phase 1 — packages/whatsapp
- Adicionado enum WaProvider { EVOLUTION, ZAPI, META_CLOUD }
- WhatsAppInstance.provider: WaProvider @default(EVOLUTION)
- WhatsAppInstance.wabaId/phoneNumberId/accessTokenEnc/webhookSecret (opcionais, Meta Cloud)
Migration: não-breaking (novos campos opcionais com default)

[2026-08-19] Painel da plataforma — apps/web
- User.isPlatformAdmin: Boolean @default(false) + índice
- Concede acesso ao /painel (cross-tenant), fora da hierarquia de Role, que é
  sempre escopada a um tenant. Habilitado só via set-platform-admin.js.
Migration: 0018_platform_admin — não-breaking (coluna nova com default false)
